version: '3.8'
services:
  main-web:
    build:
      context: ..
      dockerfile: MainWeb/Dockerfile
    ports:
    - 8080:80
    environment:
    - ASPNETCORE_ENVIRONMENT=Development
    restart: always
    networks:
    - nick-net
networks:
  nick-net:
    external: true
